# Backend runtime dependencies. Install with: pip install -r requirements.txt
# Then run: playwright install chromium

fastapi>=0.136.3
uvicorn[standard]>=0.48.0
playwright>=1.60.0
requests>=2.34.2
urllib3>=2.7.0
passlib[bcrypt]>=1.7
python-multipart>=0.0.30
jinja2>=3.1.6
pydantic>=2.13.4

# Optional PDF rendering — pick one (WeasyPrint requires GTK on Windows).
# weasyprint>=62
# pdfkit>=1.0

# Test dependencies
pytest>=8.2
httpx>=0.28.1
