[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: end-to-end test that exercises the full scan pipeline
    load: load / stress test (skipped unless a live server is running)
filterwarnings =
    ignore::DeprecationWarning
